/// <reference types="node" />
import child_process from 'child_process';
export declare const configDefaults: {
    pipe: boolean;
    fsync: string;
    pgdata: string;
    port: string;
    initSql: never[];
};
export declare type Config = typeof configDefaults;
declare function start(userConfig?: Partial<Config>): Promise<child_process.ChildProcess>;
export default start;
